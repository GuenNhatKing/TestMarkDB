from django.db import transaction
from models import *

# def check_mark(exam_id: str):
#     exam_records = Exam.objects.filter(exam_id=exam_id)
#     examinee_papers = ExamineePaper.objects.filter()
